import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Visitor | Pulse' }

export default function VisitorDetailLayout({ children }: { children: React.ReactNode }) {
  return children
}
